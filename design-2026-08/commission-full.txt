Now design the rest of the app in exactly this language. Mark has approved 5b: pure black page, one content-hugging band at #131915 on the recommendation, a 1px #253129 ring on every album sleeve, green #82bf9c as the only accent with #2e6b47 for the one filled button, #efce81 for urgency, #d4a19b for a wrong note, #788c82 for inactive, ink #e9ede7, Fraunces on the brand and titles only, four type sizes with an 11px floor, 40px row art and 72px recommendation art, structure from hairlines and spacing rather than fills. Every artboard below is 756px wide with a 393px fold marker, dark only. CRITICAL, and this is why the last app-wide port failed: a design tool draws SURFACES but an app is made of CONTROLS, so below is the GENERATED list of every control the app JS actually addresses, with its required HTML tag. Give EVERY id a visible home, keep the tag exactly as stated, and where it says MUST SUPPORT that element is read by script for that property so it has to stay a real input, select or checkbox, never a styled div. Do not invent controls that are not listed. If a control has no sensible place, put it somewhere honest and tell me, do not drop it. PLAY, the falling-notes deck and its control rail [play, 25 controls]: btn-hear <button>; btn-mem <button>; btn-perf <button>; btn-restart <button>; btn-sound <button>; btn-take <button>; btn-train <button>; chk-letters <input type=checkbox> MUST SUPPORT .checked; chunk-label <button>; chunk-next <button>; chunk-prev <button>; chunk-size <select> MUST SUPPORT .value; combo-flash <div>; falls <canvas>; hud <div>; journey-strip <div>; mode-falls <button>; mode-score <button>; perf-banner <div>; play-cover <img>; score-wrap <div>; section-select <select> MUST SUPPORT .value; tempo <input type=range> MUST SUPPORT .value; tempo-val <span>; wait-mode <input type=checkbox> MUST SUPPORT .checked. a THEORY TASK (a card drill with a mini keyboard) [task, 16 controls]: task-back <button>; task-badge <div>; task-easier <button>; task-kb <button>; task-keys <canvas>; task-msg <div>; task-payoff <button>; task-prompt <div>; task-rubric <div>; task-sheet <div>; task-show <button>; task-slots <div>; task-stage <div>; task-start <button>; task-teach <div>; task-title <h2>. a LESSON step (body text, mini keyboard, feedback) [lesson, 14 controls]: lesson-back <button>; lesson-body <p>; lesson-kb-toggle <button>; lesson-keys <canvas>; lesson-msg <div>; lesson-nomidi <div>; lesson-phase <span>; lesson-progress <span>; lesson-rhythm-link <button>; lesson-start <button>; lesson-stave <div>; lesson-steps <ol>; lesson-title <h2>; lesson-video <p>. MELODY ECHO (listen then play back) [echo, 10 controls]: echo-again <button>; echo-canvas <canvas>; echo-delta <span>; echo-level <span>; echo-mode-echo <button>; echo-mode-sing <button>; echo-mode-trans <button>; echo-msg <div>; echo-play <button>; echo-streak <span>. MY PATH (the five-skill ladder) [path, 8 controls]: path-evidence <div>; path-go <button>; path-home <button>; path-lessons <div>; path-playable <div>; path-reason <div>; path-skills <div>; path-technique <button>. TOUCH DIAGNOSTIC (velocity check) [touch, 6 controls]: touch-done <button>; touch-dyn <span>; touch-key <span>; touch-progress <div>; touch-redo <button>; touch-status <div>. METRONOME [metronome, 5 controls]: met-beat <div>; met-bpm <input type=range> MUST SUPPORT .value; met-bpm-num <input type=number> MUST SUPPORT .value; met-sig <select> MUST SUPPORT .value; met-toggle <button>. RHYTHM TAP [rhythm, 5 controls]: rhythm-blocks <div>; rhythm-go <button>; rhythm-level <span>; rhythm-msg <div>; rhythm-streak <span>. LATENCY CALIBRATION [calibrate, 4 controls]: cal-canvas <canvas>; cal-redo <button>; cal-reset <button>; cal-status <div>. IMPROV (chord backing, free play over it) [improv, 4 controls]: improv-canvas <canvas>; improv-chord <span>; improv-go <button>; improv-loop <select> MUST SUPPORT .value. TAKES (recorded performances) [takes, 3 controls]: takes-canvas <canvas>; takes-list <div>; takes-usage <div>. LESSONS (the list) [lessons, 2 controls]: btn-review <button>; lesson-list <div>. FREE PLAY (just the keyboard) [freeplay, 2 controls]: freeplay-canvas <canvas>; freeplay-log <div>. TROPHIES and the XP log [trophies, 2 controls]: trophy-list <div>; xp-log <div>. THREE OVERLAYS that float over any screen: the end-of-song RESULTS panel [results <div>; results-again <button>; results-analysis <ul>; results-done <button>; results-nudge <p>; results-score-pass <button>; results-stats <div>; results-theory <button>; results-title <h2>]; the THEORY card [theory-body <p>; theory-card <div>; theory-close <button>; theory-status <div>; theory-title <h2>]; the FIRST RUN prompt [firstrun <div>; firstrun-skip <button>; firstrun-taps <button>]. TWELVE KEYS [keys12, 4 grids built by a renderer]: a fluency ladder screen with the heading "12-key fluency ladder", a hint line, and FOUR labelled grids with these exact container ids: keys12-major (heading Major), keys12-minor (heading Natural minor), keys12-majarp (heading Major arpeggios, two octaves), keys12-minarp (heading Minor arpeggios, two octaves). Each grid is filled at runtime with 12 cells, one per key, each a button carrying the key name plus a state that reads either passed or open, so design the CELL in both states; there are 48 cells in total so they must be compact. Build these as one turn with an artboard per surface, labelled with the screen name and its control count, and tell me at the end which ids you could not place.